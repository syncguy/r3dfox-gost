name: Apply GOST Stage2 next patch

on:
  push:
    branches:
      - agent/gost-tls-poc
    paths:
      - ".github/workflows/apply-gost-stage2-next.yml"

permissions:
  contents: write
  actions: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: true

      - name: Apply integrated Stage 2 patch
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          root = Path('.')
          ns = root / 'security/manager/ssl/nsGostSSLIOLayer.cpp'
          text = ns.read_text(encoding='utf-8')

          include_anchor = '#include "GostSocketControl.h"\n'
          extra_includes = '''#include "GostSocketControl.h"\n#include "mozilla/Mutex.h"\n#include "mozilla/dom/BrowsingContext.h"\n#include "nsIClientAuthDialogService.h"\n#include "nsIClientAuthRememberService.h"\n#include "nsIEventTarget.h"\n#include "nsISocketTransportService.h"\n#include "nsIX509Cert.h"\n#include "nsIX509CertDB.h"\n#include "nsServiceManagerUtils.h"\n#include "nsThreadUtils.h"\n'''
          if include_anchor not in text:
              raise SystemExit('include anchor missing')
          text = text.replace(include_anchor, extra_includes, 1)

          insertion_anchor = 'int SelectStage1ClientCertificate(void* aArg) {\n'
          if insertion_anchor not in text:
              raise SystemExit('client callback anchor missing')

          picker = r'''
#ifdef XP_WIN
enum class GostClientCertPhase { Selecting, Selected, Declined, Failed };

class GostClientCertState final
    : public mozilla::RefCountedThreadSafe<GostClientCertState> {
 public:
  GostClientCertState(MSSPI_HANDLE aMsspi, GostSocketControl* aControl,
                      const nsACString& aHost,
                      const mozilla::OriginAttributes& aOriginAttributes,
                      uint64_t aBrowserId,
                      nsTArray<nsTArray<uint8_t>>&& aCandidates,
                      nsTArray<nsTArray<uint8_t>>&& aCANames)
      : mMutex("GostClientCertState"),
        mMsspi(aMsspi),
        mControl(aControl),
        mHost(aHost),
        mOriginAttributes(aOriginAttributes),
        mBrowserId(aBrowserId),
        mCandidates(std::move(aCandidates)),
        mCANames(std::move(aCANames)) {}

  mozilla::Mutex mMutex;
  MSSPI_HANDLE mMsspi;
  RefPtr<GostSocketControl> mControl;
  nsCString mHost;
  mozilla::OriginAttributes mOriginAttributes;
  uint64_t mBrowserId;
  nsTArray<nsTArray<uint8_t>> mCandidates;
  nsTArray<nsTArray<uint8_t>> mCANames;
  GostClientCertPhase mPhase = GostClientCertPhase::Selecting;
  nsTArray<uint8_t> mSelectedDER;

 private:
  friend class mozilla::RefCountedThreadSafe<GostClientCertState>;
  ~GostClientCertState() = default;
};

struct GostRememberedClientCert {
  nsCString key;
  nsTArray<uint8_t> der;
  bool declined = false;
};

static mozilla::StaticMutex sClientCertSelectionMutex;
static mozilla::StaticAutoPtr<nsTArray<RefPtr<GostClientCertState>>>
    sClientCertSelections;
static mozilla::StaticAutoPtr<nsTArray<GostRememberedClientCert>>
    sRememberedClientCerts;

nsCString GostClientCertDecisionKey(
    const nsACString& aHost,
    const mozilla::OriginAttributes& aOriginAttributes) {
  nsCString key(aHost);
  key.Append('\0');
  nsAutoCString suffix;
  aOriginAttributes.CreateSuffix(suffix);
  key.Append(suffix);
  return key;
}

RefPtr<GostClientCertState> FindGostClientCertState(MSSPI_HANDLE aMsspi) {
  mozilla::StaticMutexAutoLock lock(sClientCertSelectionMutex);
  if (!sClientCertSelections) {
    return nullptr;
  }
  for (const auto& state : *sClientCertSelections) {
    if (state->mMsspi == aMsspi) {
      return state;
    }
  }
  return nullptr;
}

void RemoveGostClientCertState(MSSPI_HANDLE aMsspi) {
  mozilla::StaticMutexAutoLock lock(sClientCertSelectionMutex);
  if (!sClientCertSelections) {
    return;
  }
  for (uint32_t i = 0; i < sClientCertSelections->Length(); ++i) {
    if ((*sClientCertSelections)[i]->mMsspi == aMsspi) {
      sClientCertSelections->RemoveElementAt(i);
      return;
    }
  }
}

bool FindRememberedGostClientCert(const nsACString& aKey,
                                  nsTArray<uint8_t>& aDER,
                                  bool& aDeclined) {
  mozilla::StaticMutexAutoLock lock(sClientCertSelectionMutex);
  if (!sRememberedClientCerts) {
    return false;
  }
  for (const auto& choice : *sRememberedClientCerts) {
    if (choice.key.Equals(aKey)) {
      aDER = choice.der.Clone();
      aDeclined = choice.declined;
      return true;
    }
  }
  return false;
}

void RememberGostClientCert(const nsACString& aKey,
                            const nsTArray<uint8_t>& aDER,
                            bool aDeclined) {
  mozilla::StaticMutexAutoLock lock(sClientCertSelectionMutex);
  if (!sRememberedClientCerts) {
    sRememberedClientCerts = new nsTArray<GostRememberedClientCert>();
  }
  for (auto& choice : *sRememberedClientCerts) {
    if (choice.key.Equals(aKey)) {
      choice.der = aDER.Clone();
      choice.declined = aDeclined;
      return;
    }
  }
  GostRememberedClientCert choice;
  choice.key = aKey;
  choice.der = aDER.Clone();
  choice.declined = aDeclined;
  sRememberedClientCerts->AppendElement(std::move(choice));
}

bool GostCertNameEquals(const CERT_NAME_BLOB& aName,
                        const nsTArray<uint8_t>& aCAName) {
  return aName.cbData == aCAName.Length() && aName.pbData &&
         memcmp(aName.pbData, aCAName.Elements(), aCAName.Length()) == 0;
}

bool GostClientCertMatchesCANames(
    PCCERT_CONTEXT aCert, const nsTArray<nsTArray<uint8_t>>& aCANames) {
  if (aCANames.IsEmpty()) {
    return true;
  }

  CERT_CHAIN_PARA para = {};
  para.cbSize = sizeof(para);
  PCCERT_CHAIN_CONTEXT chain = nullptr;
  if (!CertGetCertificateChain(
          nullptr, aCert, nullptr, aCert->hCertStore, &para,
          CERT_CHAIN_CACHE_END_CERT | CERT_CHAIN_CACHE_ONLY_URL_RETRIEVAL,
          nullptr, &chain) ||
      !chain) {
    return false;
  }

  bool matched = false;
  for (DWORD chainIndex = 0; chainIndex < chain->cChain && !matched;
       ++chainIndex) {
    PCERT_SIMPLE_CHAIN simple = chain->rgpChain[chainIndex];
    for (DWORD element = 0; element < simple->cElement && !matched; ++element) {
      PCCERT_CONTEXT current = simple->rgpElement[element]->pCertContext;
      for (const auto& caName : aCANames) {
        if (GostCertNameEquals(current->pCertInfo->Subject, caName) ||
            GostCertNameEquals(current->pCertInfo->Issuer, caName)) {
          matched = true;
          break;
        }
      }
    }
  }
  CertFreeCertificateChain(chain);
  return matched;
}

bool CollectGostCANames(MSSPI_HANDLE aMsspi,
                        nsTArray<nsTArray<uint8_t>>& aCANames) {
  size_t count = 0;
  if (!msspi_get_issuerlist(aMsspi, nullptr, nullptr, &count)) {
    return false;
  }
  if (!count) {
    return true;
  }
  nsTArray<const uint8_t*> buffers;
  nsTArray<size_t> lengths;
  buffers.SetLength(count);
  lengths.SetLength(count);
  size_t actualCount = count;
  if (!msspi_get_issuerlist(aMsspi, buffers.Elements(), lengths.Elements(),
                            &actualCount)) {
    return false;
  }
  for (size_t i = 0; i < actualCount; ++i) {
    nsTArray<uint8_t> name;
    name.AppendElements(buffers[i], lengths[i]);
    aCANames.AppendElement(std::move(name));
  }
  return true;
}

bool CollectGostClientCertCandidates(
    const nsTArray<nsTArray<uint8_t>>& aCANames,
    nsTArray<nsTArray<uint8_t>>& aCandidates) {
  HCERTSTORE store = CertOpenStore(
      CERT_STORE_PROV_SYSTEM_W, 0, 0,
      CERT_SYSTEM_STORE_CURRENT_USER | CERT_STORE_OPEN_EXISTING_FLAG |
          CERT_STORE_READONLY_FLAG,
      L"MY");
  if (!store) {
    return false;
  }

  PCCERT_CONTEXT cert = nullptr;
  while ((cert = CertEnumCertificatesInStore(store, cert))) {
    DWORD keyProviderInfoSize = 0;
    if (!CertGetCertificateContextProperty(cert, CERT_KEY_PROV_INFO_PROP_ID,
                                           nullptr, &keyProviderInfoSize) ||
        !keyProviderInfoSize) {
      continue;
    }
    if (!GostClientCertMatchesCANames(cert, aCANames)) {
      continue;
    }
    nsTArray<uint8_t> der;
    der.AppendElements(cert->pbCertEncoded, cert->cbCertEncoded);
    aCandidates.AppendElement(std::move(der));
  }
  CertCloseStore(store, 0);
  return true;
}

bool GostClientCertStateIsActive(GostClientCertState* aState) {
  mozilla::StaticMutexAutoLock lock(sClientCertSelectionMutex);
  if (!sClientCertSelections) {
    return false;
  }
  for (const auto& state : *sClientCertSelections) {
    if (state == aState) {
      return true;
    }
  }
  return false;
}

void WakeGostClientCertHandshake(GostClientCertState* aState) {
  nsCOMPtr<nsIEventTarget> socketThread(
      do_GetService(NS_SOCKETTRANSPORTSERVICE_CONTRACTID));
  if (!socketThread) {
    return;
  }
  RefPtr<GostClientCertState> state(aState);
  (void)socketThread->Dispatch(
      NS_NewRunnableFunction("GostClientAuthResume", [state]() {
        if (!GostClientCertStateIsActive(state)) {
          return;
        }
        (void)state->mControl->DriveHandshake();
      }));
}

class GostClientAuthDialogCallback final
    : public nsIClientAuthDialogCallback {
 public:
  NS_DECL_THREADSAFE_ISUPPORTS
  NS_DECL_NSICLIENTAUTHDIALOGCALLBACK

  explicit GostClientAuthDialogCallback(GostClientCertState* aState)
      : mState(aState) {}

 private:
  ~GostClientAuthDialogCallback() = default;
  RefPtr<GostClientCertState> mState;
};

NS_IMPL_ISUPPORTS(GostClientAuthDialogCallback, nsIClientAuthDialogCallback)

NS_IMETHODIMP GostClientAuthDialogCallback::CertificateChosen(
    nsIX509Cert* aCert,
    nsIClientAuthRememberService::Duration aRememberDuration) {
  nsTArray<uint8_t> der;
  if (aCert) {
    nsresult rv = aCert->GetRawDER(der);
    if (NS_FAILED(rv)) {
      {
        mozilla::MutexAutoLock lock(mState->mMutex);
        mState->mPhase = GostClientCertPhase::Failed;
      }
      WakeGostClientCertHandshake(mState);
      return rv;
    }
  }

  {
    mozilla::MutexAutoLock lock(mState->mMutex);
    mState->mSelectedDER = der.Clone();
    mState->mPhase = aCert ? GostClientCertPhase::Selected
                           : GostClientCertPhase::Declined;
  }
  if (aRememberDuration != nsIClientAuthRememberService::Once) {
    RememberGostClientCert(
        GostClientCertDecisionKey(mState->mHost, mState->mOriginAttributes),
        der, !aCert);
  }
  WakeGostClientCertHandshake(mState);
  return NS_OK;
}

void OpenGostClientAuthDialog(GostClientCertState* aState) {
  MOZ_ASSERT(NS_IsMainThread());
  RefPtr<GostClientCertState> state(aState);
  nsCOMPtr<nsIX509CertDB> certDB(do_GetService(NS_X509CERTDB_CONTRACTID));
  nsCOMPtr<nsIClientAuthDialogService> dialog(
      do_GetService(NS_CLIENTAUTHDIALOGSERVICE_CONTRACTID));
  if (!certDB || !dialog) {
    {
      mozilla::MutexAutoLock lock(state->mMutex);
      state->mPhase = GostClientCertPhase::Failed;
    }
    WakeGostClientCertHandshake(state);
    return;
  }

  nsTArray<RefPtr<nsIX509Cert>> certArray;
  for (const auto& der : state->mCandidates) {
    nsCOMPtr<nsIX509Cert> cert;
    if (NS_SUCCEEDED(certDB->ConstructX509(der, getter_AddRefs(cert))) && cert) {
      certArray.AppendElement(cert);
    }
  }
  if (certArray.IsEmpty()) {
    {
      mozilla::MutexAutoLock lock(state->mMutex);
      state->mPhase = GostClientCertPhase::Declined;
    }
    WakeGostClientCertHandshake(state);
    return;
  }

  RefPtr<mozilla::dom::BrowsingContext> browsingContext;
  if (state->mBrowserId) {
    browsingContext =
        mozilla::dom::BrowsingContext::GetCurrentTopByBrowserId(state->mBrowserId);
  }
  RefPtr<nsIClientAuthDialogCallback> callback(
      new GostClientAuthDialogCallback(state));
  nsresult rv = dialog->ChooseCertificate(state->mHost, certArray,
                                          browsingContext, state->mCANames,
                                          callback);
  if (NS_FAILED(rv)) {
    {
      mozilla::MutexAutoLock lock(state->mMutex);
      state->mPhase = GostClientCertPhase::Failed;
    }
    WakeGostClientCertHandshake(state);
  }
}

int SelectFirefoxGostClientCertificate(GostSecret* aSecret,
                                       const nsACString& aHost) {
  const mozilla::OriginAttributes originAttributes(
      aSecret->control->GetOriginAttributes());
  const nsCString decisionKey(
      GostClientCertDecisionKey(aHost, originAttributes));

  nsTArray<uint8_t> rememberedDER;
  bool rememberedDeclined = false;
  if (FindRememberedGostClientCert(decisionKey, rememberedDER,
                                   rememberedDeclined)) {
    if (rememberedDeclined) {
      MOZ_LOG(gGostTLSLog, mozilla::LogLevel::Info,
              ("client certificate remembered host=%s selected=0 scope=session",
               PromiseFlatCString(aHost).get()));
      return 1;
    }
    const int selected = msspi_set_mycert(
        aSecret->msspi, rememberedDER.Elements(), rememberedDER.Length());
    if (selected) {
      aSecret->clientCertLoaded = true;
    }
    MOZ_LOG(gGostTLSLog, mozilla::LogLevel::Info,
            ("client certificate remembered host=%s selected=%d scope=session",
             PromiseFlatCString(aHost).get(), selected ? 1 : 0));
    return selected ? 1 : 0;
  }

  RefPtr<GostClientCertState> state(
      FindGostClientCertState(aSecret->msspi));
  if (state) {
    GostClientCertPhase phase;
    nsTArray<uint8_t> der;
    {
      mozilla::MutexAutoLock lock(state->mMutex);
      phase = state->mPhase;
      der = state->mSelectedDER.Clone();
    }
    if (phase == GostClientCertPhase::Selecting) {
      return -1;
    }
    if (phase == GostClientCertPhase::Declined) {
      RemoveGostClientCertState(aSecret->msspi);
      MOZ_LOG(gGostTLSLog, mozilla::LogLevel::Info,
              ("client certificate dialog completed host=%s selected=0",
               PromiseFlatCString(aHost).get()));
      return 1;
    }
    if (phase == GostClientCertPhase::Failed || der.IsEmpty()) {
      RemoveGostClientCertState(aSecret->msspi);
      return 0;
    }
    const int selected =
        msspi_set_mycert(aSecret->msspi, der.Elements(), der.Length());
    RemoveGostClientCertState(aSecret->msspi);
    if (selected) {
      aSecret->clientCertLoaded = true;
    }
    MOZ_LOG(gGostTLSLog, mozilla::LogLevel::Info,
            ("client certificate dialog completed host=%s selected=%d",
             PromiseFlatCString(aHost).get(), selected ? 1 : 0));
    return selected ? 1 : 0;
  }

  nsTArray<nsTArray<uint8_t>> caNames;
  if (!CollectGostCANames(aSecret->msspi, caNames)) {
    return 0;
  }
  nsTArray<nsTArray<uint8_t>> candidates;
  if (!CollectGostClientCertCandidates(caNames, candidates)) {
    return 0;
  }
  MOZ_LOG(gGostTLSLog, mozilla::LogLevel::Info,
          ("client certificate candidates host=%s count=%zu mode=firefox-ui",
           PromiseFlatCString(aHost).get(), candidates.Length()));
  if (candidates.IsEmpty()) {
    return 1;
  }

  uint64_t browserId = 0;
  (void)aSecret->control->GetBrowserId(&browserId);
  state = new GostClientCertState(aSecret->msspi, aSecret->control, aHost,
                                  originAttributes, browserId,
                                  std::move(candidates), std::move(caNames));
  {
    mozilla::StaticMutexAutoLock lock(sClientCertSelectionMutex);
    if (!sClientCertSelections) {
      sClientCertSelections = new nsTArray<RefPtr<GostClientCertState>>();
    }
    sClientCertSelections->AppendElement(state);
  }
  MOZ_LOG(gGostTLSLog, mozilla::LogLevel::Info,
          ("client certificate dialog requested host=%s mode=firefox-ui",
           PromiseFlatCString(aHost).get()));
  (void)NS_DispatchToMainThread(NS_NewRunnableFunction(
      "GostClientAuthDialog", [state]() { OpenGostClientAuthDialog(state); }));
  return -1;
}
#endif

'''
          text = text.replace(insertion_anchor, picker + insertion_anchor, 1)

          start = text.find('  if (secret->clientCertThumbprint.IsEmpty()) {\n')
          if start < 0:
              raise SystemExit('missing-selector start missing')
          end = text.find('\n#ifdef XP_WIN\n  uint8_t hash[kSha1ThumbprintBytes];', start)
          if end < 0:
              raise SystemExit('missing-selector end missing')
          new_missing = '''  if (secret->clientCertThumbprint.IsEmpty()) {\n#ifdef XP_WIN\n    return SelectFirefoxGostClientCertificate(secret, host);\n#else\n    MOZ_LOG(gGostTLSLog, mozilla::LogLevel::Error,\n            ("client certificate selection unavailable host=%s", host.get()));\n    return 0;\n#endif\n  }\n'''
          text = text[:start] + new_missing + text[end:]

          close_anchor = '    msspi_close(secret->msspi);\n'
          if close_anchor not in text:
              raise SystemExit('msspi close anchor missing')
          text = text.replace(
              close_anchor,
              '#ifdef XP_WIN\n    RemoveGostClientCertState(secret->msspi);\n#endif\n' + close_anchor,
              1)
          ns.write_text(text, encoding='utf-8')

          workflows = [
              root / '.github/workflows/gost-ssl-compile-check.yml',
              root / '.github/workflows/gost-poc-build.yml',
              root / '.github/workflows/gost-poc-build-thunk.yml',
          ]
          old_expr = "$source.Replace('#include <Windows.h>', '#include <windows.h>')"
          new_expr = "$source.Replace('#include <Windows.h>', '#include <windows.h>').Replace('SECPKG_ATTR_REMOTE_CERT_CHAIN', 'SECPKG_ATTR_REMOTE_CERT_CONTEXT')"
          for workflow in workflows:
              contents = workflow.read_text(encoding='utf-8')
              if old_expr not in contents:
                  raise SystemExit(f'MSSPI replacement anchor missing in {workflow}')
              contents = contents.replace(old_expr, new_expr, 1)
              workflow.write_text(contents, encoding='utf-8')

          plan = root / 'docs/gost/STAGE2_PLAN.md'
          contents = plan.read_text(encoding='utf-8')
          marker = '## Stage 2.2 — server trust decision and positive session cache\n'
          addition = '''## Current combined implementation experiment\n\nThe next build combines two useful changes so a full Firefox build is not spent on a one-line compatibility probe:\n\n- Windows MSSPI peer-certificate acquisition is switched directly from `SECPKG_ATTR_REMOTE_CERT_CHAIN` to `SECPKG_ATTR_REMOTE_CERT_CONTEXT`; the existing MSSPI `CertGetCertificateChain` path remains responsible for chain construction. There is no per-connection capability-probe fallback.\n- `R3DFOX_GOST_CLIENT_CERT_THUMBPRINT` remains the first and unchanged known-good client-certificate path. When it is absent, the GOST callback uses an asynchronous Firefox `nsIClientAuthDialogService` path: `MSSPI_X509_LOOKUP -> would-block -> main-thread clientauthask.xhtml -> socket-thread resume -> msspi_set_mycert`.\n- UI candidates come from `CurrentUser\\\\MY`, require a private-key provider binding, and are filtered against the server acceptable-CA DER names by matching subject/issuer names in the locally built certificate chain.\n- The first UI implementation reuses a selected or declined choice in process memory when the dialog requests remembering it. Persistent integration with Firefox's remember database remains later hardening work.\n\n'''
          if marker not in contents:
              raise SystemExit('STAGE2_PLAN marker missing')
          plan.write_text(contents.replace(marker, addition + marker, 1), encoding='utf-8')

          testlog = root / 'docs/gost/TEST_LOG.md'
          contents = testlog.read_text(encoding='utf-8')
          contents += '''\n---\n\n## 2026-08-25 — Stage 2.1 main runtime localizes verifier failure and validates issuer-list dedup\n\n**Track:** GOST TLS runtime / Stage 2.1 observability  \n**Branch:** `agent/gost-tls-poc`  \n**Code-under-test:** `c62022a5530a61124b756648293113187b8e5b8b`  \n**Main build:** run `32810337957`, job `97688347771`  \n**Runtime result:** browser-visible Treasury use succeeds without lag or visible errors\n\n### Sanitized observation\n\nThe main-build runtime capture contains 12 completed GOST handshakes for `fzs.roskazna.ru` and 13 completed mTLS handshakes for `lk-fzs.roskazna.ru`. Login-host handshakes continue to report `client_cert_loaded=1`; the previous `0x80090326` failure does not recur and no `E/GostTLS` entry is present.\n\nThe acceptable-issuer diagnostics return 34 entries / 11,420 DER bytes. The complete list is decoded once (`already_logged=0`) and 13 later identical observations are suppressed with `already_logged=1`, confirming browser-session dedup. All 34 entries decode as X.500 names; identifying DN values are intentionally not recorded here.\n\nAcross completed connections, server peer-certificate acquisition fails consistently with `0x80090302` (`SEC_E_UNSUPPORTED_FUNCTION`) on pinned MSSPI's Windows request for `SECPKG_ATTR_REMOTE_CERT_CHAIN`. MSSPI therefore has no `peercert`, peer-chain/name helpers cannot produce data, and `msspi_get_verify_status()` returns its internal-failure form (`ok=0`). This localizes the current verifier blocker to peer-certificate acquisition rather than to a proved Treasury certificate-policy failure.\n\n### Conclusion\n\n**Stage 2.1 observability succeeded and changed the leading blocker.** The next compatibility experiment uses the already-existing MSSPI `SECPKG_ATTR_REMOTE_CERT_CONTEXT` leaf-certificate path on Windows and lets `CertGetCertificateChain` build the chain. Detailed issuer diagnostics are also confirmed usable without browser-visible performance degradation.\n'''
          testlog.write_text(contents, encoding='utf-8')

          state = root / 'docs/gost/PROJECT_STATE.md'
          contents = state.read_text(encoding='utf-8')
          marker = 'Successful ordinary and Stage 1 Treasury sessions still do not establish fail-closed server-certificate validation. Earlier successful sessions contain:\n'
          replacement = '''Stage 2.1 runtime diagnostics on the main full build (run `32810337957`, job `97688347771`, source SHA `c62022a5530a61124b756648293113187b8e5b8b`) localize the current verification failure: pinned MSSPI requests `SECPKG_ATTR_REMOTE_CERT_CHAIN` on Windows and the active SSPI/CryptoPro provider returns `0x80090302` (`SEC_E_UNSUPPORTED_FUNCTION`). MSSPI therefore never obtains `peercert`, so its peer-chain/name helpers and `msspi_get_verify_status()` reach the internal-error path before certificate policy is evaluated. The next implementation uses `SECPKG_ATTR_REMOTE_CERT_CONTEXT` and the existing `CertGetCertificateChain` verifier path.\n\nThe same runtime capture confirms the 34-entry acceptable-CA list is fully decoded once and repeated identical lists are suppressed for the rest of the browser session. Browser-visible Treasury use, including mTLS, remains successful.\n\nSuccessful ordinary and Stage 1 Treasury sessions still do not yet establish fail-closed server-certificate validation. Earlier successful sessions contain:\n'''
          if marker not in contents:
              raise SystemExit('PROJECT_STATE marker missing')
          state.write_text(contents.replace(marker, replacement, 1), encoding='utf-8')

          todo = root / 'docs/gost/TODO.md'
          contents = todo.read_text(encoding='utf-8')
          old = '3. **CURRENT — Stage 2:** diagnose and close fail-open server-certificate verification first.\n4. **NEXT — Stage 2:** resolve acceptable-issuer/final certificate-selection policy and Firefox-facing UX.\n'
          new = '3. **DONE — Stage 2.1 diagnosis:** `REMOTE_CERT_CHAIN` is unsupported by the active SSPI/CryptoPro provider; the next implementation switches directly to `REMOTE_CERT_CONTEXT` and existing `CertGetCertificateChain`.\n4. **CURRENT — Stage 2 implementation:** combine peer-certificate acquisition repair with the first asynchronous Firefox-facing client-certificate picker while retaining the known-good thumbprint selector as the priority diagnostic path.\n'
          if old not in contents:
              raise SystemExit('TODO sequence marker missing')
          todo.write_text(contents.replace(old, new, 1), encoding='utf-8')

          (root / '.github/workflows/apply-gost-stage2-next.yml').unlink()
          PY

      - name: Commit integrated change
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(gost): add firefox client cert selection path"
          git push origin HEAD:agent/gost-tls-poc
          for branch in agent/gost-stage2-next-temp agent/gost-stage2-next-temp2 agent/gost-stage2-next-temp3 agent/gost-stage2-next-temp4 agent/gost-stage2-next-temp5; do
            git push origin --delete "$branch" || true
          done

      - name: Dispatch GOST builds
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          gh workflow run gost-ssl-compile-check.yml --ref agent/gost-tls-poc
          gh workflow run gost-poc-build.yml --ref agent/gost-tls-poc
          gh workflow run gost-poc-build-thunk.yml --ref agent/gost-tls-poc
