[package]
name = "widget_windows"
version = "0.1.0"
edition = "2021"

# XP legacy build: this crate intentionally has no Windows Runtime dependency.

[dependencies]
libc = "0.2"
nserror = { path = "../../../xpcom/rust/nserror" }
nsstring = { path = "../../../xpcom/rust/nsstring" }
thin-vec = { version = "0.2.1", features = ["gecko-ffi"] }
xpcom = { path = "../../../xpcom/rust/xpcom" }
