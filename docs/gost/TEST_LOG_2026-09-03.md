# r3dfox — Experiment Log — 2026-09-03

## XP KERNEL32 Application Restart + named-pipe source-remediation smoke — GREEN

**Track:** Windows XP SP3 x86 compatibility / remaining source-remediation KERNEL32 quartet  
**Branch:** `agent/winrt-source-poc`  
**Source-under-test:** `0450fd8f2b22b9e0263e0755e0ea52f4dd6e2aa4` (`ci(xp): fix dumpbin import parser in remediation smoke`)  
**Workflow:** `XP KERNEL32 source remediation smoke` (`.github/workflows/xp-kernel32-source-remediation-smoke.yml`)  
**Actions run:** `33720100459`  
**Job:** `100537300030` (`Application Restart + named pipe / XP x86`)  
**Result:** success  
**Diagnostics artifact:** `9879912839` (`xp-kernel32-source-remediation-diagnostics`), digest `sha256:29f742d11f584a07695fcb5cfa87d5f7046e5a22d54470f3b97acb065b85b886`

### Ownership established

The focused source-anchor gate confirms the Firefox 153 owners used for this experiment:

- `GetApplicationRestartSettings`, `RegisterApplicationRestart`, and `UnregisterApplicationRestart` are owned by `toolkit/xre/nsAppRunner.cpp::RegisterApplicationRestartChanged` and are tied to `PREF_WIN_REGISTER_APPLICATION_RESTART` through `Preferences::RegisterCallbackAndCall`;
- `GetNamedPipeServerProcessId` is owned by `accessible/windows/msaa/CompatibilityUIA.cpp::GetUiaClientPidsWin11::QueryThreadProc`, reached from `Compatibility::GetUiaClientPids` only on the `IsWin11OrLater()` path, while older Windows uses `GetUiaClientPidWin10`.

The Application Restart trio is optional Windows restart/relaunch integration rather than a normal browser startup/runtime prerequisite. The named-pipe API belongs to Windows 11 UI Automation client detection, not Firefox sandbox IPC.

### Remediation model under test

The representative legacy-XP variant implements the chosen policies:

- Application Restart trio: compile-time no-op for the legacy XP configuration, removing the optional feature as one unit rather than supplying three thunks;
- `GetNamedPipeServerProcessId`: resolve the real KERNEL32 export with `GetModuleHandleW` + `GetProcAddress`; if unavailable, return failure rather than fabricating a PID. When available, call the native API unchanged.

No broad YY `kernel32.lib` and no mechanical stub is involved.

### Proof chain

The exact run passed:

1. sparse checkout of the two source owners;
2. fail-closed source ownership/call-path anchor validation;
3. x86 compilation and link of both the positive control and remediation probe;
4. positive-control import proof — all four APIs remain direct hard KERNEL32 imports;
5. remediation import proof — none of the four APIs remains a direct hard import;
6. x86 / PE subsystem 5.01 gate;
7. hosted Windows runtime sanity for the remediation variant;
8. diagnostics artifact upload.

### Preceding parser-only RED

Run `33719991764`, job `100536981774`, source `95f6a680d3be006a9c9224934562e732b3e2039b` failed only at the import-parser gate. Its diagnostics already showed all four APIs present in the control and absent in the remediation binary. The parser incorrectly expected a bare API row while `dumpbin /imports` emitted an ordinal followed by the API name. Commit `0450fd8f...` corrected only that parser shape and the rerun became GREEN; remediation semantics did not change.

### Conclusion / evidence boundary

**REPRESENTATIVE SOURCE-REMEDIATION STRATEGY GREEN.** This focused experiment proves that the selected source policies can eliminate this four-API hard-import quartet at representative x86 link scale while retaining the intended modern named-pipe behavior through runtime resolution.

This does **not** yet prove production Firefox object compilation, final `xul.dll` import closure, or physical Windows XP runtime. The next step is production source integration followed by exact affected-target/object inspection; the existing inventory-driven full-browser PE audit and physical XP startup progression remain later acceptance gates.

Status: current focused evidence. No GOST TLS runtime conclusion follows from this experiment.
