# Windows XP residual import remediation — canonical location

The authoritative document is maintained on default branch `agent/gost-tls-poc` at `docs/gost/XP_IMPORT_REMEDIATION.md`.

This branch-local pointer exists only for reliable context recovery on `agent/winrt-source-poc`. Read the canonical file for the durable remediation matrix, then use branch-local `PROJECT_STATE.md`, `TEST_LOG.md`, current code and exact CI/runtime evidence for newer experiment state.
