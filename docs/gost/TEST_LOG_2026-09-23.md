# r3dfox GOST TLS — Experiment Log — 2026-09-23

## WebRTC XP runtime boundary: first enabled build blocked; nICEr fallback build starts and passes local DataChannel

**Track:** Windows XP SP3 x86 compatibility / WebRTC runtime  
**Branch:** `agent/winrt-source-poc`  
**Browser line:** r3dfox / Firefox 153 x86

### A. First WebRTC-enabled full build — CI GREEN, physical XP BLOCKED

**Source-under-test:** `75b4e8f052fb6fc09c723651938fde18f95af4ea` (`ci(xp): enable WebRTC in full x86 build`)  
**Workflow:** `GOST TLS PoC build  XP x32` (`.github/workflows/gost-poc-build-xp-x32.yml`)  
**Actions run:** `35706851492`  
**Job:** `106677750169`  
**Result:** `completed / success`  
**Package artifact:** `10691966451` (`r3dfox-gost-xp-x32-package`)  
**Physical-test runtime artifact:** `10692226247` (`r3dfox-gost-xp-x32-runtime`)  
**Diagnostics artifact:** `10691613026` (`r3dfox-gost-xp-x32-diagnostics`)

This was the first full XP x86 build in this sequence with `ac_add_options --enable-webrtc`. Compile, link, package and existing static XP gates passed.

Physical Windows XP execution did **not** pass. The produced browser did not successfully start on physical XP, so no `RTCPeerConnection`, ICE, DataChannel, camera/microphone or RTP runtime result may be attributed to this run.

The newly compiled WebRTC/nICEr path exposed an XP-incompatible `inet_pton` dependency in `dom/media/webrtc/transport/third_party/nICEr/src/net/transport_addr.cpp`. This was the next physical-XP startup blocker.

**Conclusion:** WebRTC-enabled build/package/static gates PASS; physical-XP startup BLOCKED; WebRTC runtime NOT TESTED.

### B. Immediate fix — XP-local nICEr `inet_pton` fallback

**Source:** `afee8c9e5ad2da729407ae06cda8d8029895ab06` (`fix(xp): add nICEr inet_pton fallback`)

This commit is exactly one commit ahead of `75b4e8f...` and changes only:

`dom/media/webrtc/transport/third_party/nICEr/src/net/transport_addr.cpp`

Under `MOZ_XP_COMPAT`, the nICEr address conversion path uses a local IPv4/IPv6 parser instead of the unavailable XP `inet_pton` path. Non-XP builds retain native `inet_pton`.

### C. Corrected full build — physical XP startup PASS

**Source-under-test:** `afee8c9e5ad2da729407ae06cda8d8029895ab06`  
**Actions run:** `35737946733`  
**Job:** `106779925555`  
**Result:** `completed / success`  
**Package artifact:** `10707883013` (`r3dfox-gost-xp-x32-package`)  
**Physical-test runtime artifact:** `10707967905` (`r3dfox-gost-xp-x32-runtime`)  
**Diagnostics artifact:** `10707868191` (`r3dfox-gost-xp-x32-diagnostics`)

The resulting browser started successfully on physical Windows XP SP3 x86.

Physical tested-tree identity:

- `SourceStamp=afee8c9e5ad2da729407ae06cda8d8029895ab06`
- `r3dfox.exe` SHA-1 `b1e38de25a5212a54833ddcd4ca830318a10467c`
- `xul.dll` SHA-1 `266b8baea04e92d301fb6ffdd5ad87f492c398eb`

### D. WebRTC runtime observations on physical XP

API sanity:

- `typeof RTCPeerConnection` => `"function"`
- `typeof navigator.mediaDevices?.getUserMedia` => `"function"`
- `about:webrtc` is functional and reports real PeerConnection instances.

A manually created PeerConnection/DataChannel generated SDP with UDP/DTLS/SCTP, DTLS SHA-256 fingerprint, SCTP port, ICE credentials, UDP/TCP host candidates and end-of-candidates. ICE gathering therefore runs successfully on physical XP.

A two-peer local loopback test then completed:

- both ICE states reached `connected`;
- both PeerConnection `connectionState` values reached `connected`;
- both DataChannels reached OPEN;
- application payload `Hello from Windows XP WebRTC` was delivered to the receiving channel.

**Conclusion:** for exact source `afee8c9e...` / run `35737946733`, physical-XP browser startup PASS and WebRTC PeerConnection + ICE + DTLS/SCTP DataChannel payload transfer PASS.

### Evidence boundary / open work

This does not yet prove:

- external-peer connectivity through NAT;
- STUN/TURN traversal;
- camera or microphone capture;
- RTP/RTCP audio/video transport;
- actual H.264/VP8/VP9/AV1 encode/decode on XP.

Do not conflate API exposure or codec capability advertisement with media runtime proof.

See `WEBRTC_XP_STATUS.md` for the current concise status and artifact-selection rule.
