# Windows XP synchronization import status — canonical location

The authoritative document is maintained on default branch `agent/gost-tls-poc` at `docs/gost/XP_SYNC_IMPORT_STATUS.md`.

This branch-local pointer prevents false "not found" conclusions during context recovery on `agent/winrt-source-poc`. Read the canonical default-branch document for the durable status, then use this branch's `PROJECT_STATE.md`, `TEST_LOG.md`, current code and exact CI/runtime evidence for newer experiment state.
